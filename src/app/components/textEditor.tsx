import React from 'react';
import styles from './textEditor.module.css';

const TextEditor: React.FC = () => {
  return <div className={styles.textEditorWrapper}>TextEditor</div>;
};

export default TextEditor;
